# 中继双向通信

[English](./DESIGN-relay.md)

> **已实现。** 本文记录推导过程；代码在 `internal/relay/` 和
> `internal/node/relayd.go`。

## 问题

数据面交给 sing-box，而 **sing-box 的 VLESS/Reality 是严格单向的**：
只能客户端拨服务端。

```
叶子 → 中继            ✓  叶子主动拨
中继 → 叶子            ✗  叶子没有 endpoint，无从拨起
叶子A → 中继 → 叶子B   ✗  卡在第二跳
```

最容易想到的绕法是给每个叶子也配 endpoint，把所有节点都变成中继 ——
**那等于每台机器都开一个入站隧道口**，恰好否定了当初选 Reality 的理由。
IP 池小的时候，每多一个暴露的端口都是负担。

## 目标

叶子**永远只出站**，不监听任何端口。任意两个叶子之间仍能经中继互通。

```
叶子A ──Reality──> 中继 <──Reality── 叶子B
       <═════════        ═════════>
       同一条连接上双向多路复用
```

## 关键决定：不自己实现 Reality

第一反应是让 knot 自己说 Reality —— `github.com/metacubex/utls` 两端都以 Go
库的形式提供，knot 直接 import 就能绕开 sing-box。

**最后走了相反的路：控制会话跑在 sing-box 已经建好的 Reality 隧道里。**

```
叶子 knot ──拨 中继VIP:9997──> sing-box ──Reality──> 中继 sing-box ──> 中继 knot
                                                     ↑ 常驻，双向 yamux
```

叶子拨的是中继的 **mesh 地址**，被自己的 TUN 捕获，经现有的 Reality 出站送出去，
中继那边的 sing-box 把明文交给 knot。于是 `internal/relay/` 里**一行 TLS 代码
都没有** —— 不用维护第二套 Reality 实现，也不会出现第二套需要对齐的指纹。

## 协议

刻意做得很小，因为 yamux 已经处理了分帧、顺序和流控。

### 会话建立

1. 叶子拨中继的 mesh 地址，发 `HELLO{nodeID, key}`
2. 中继拿 `sha256(key)` 和 head 下发的哈希表比对，通过则把连接登记进
   `nodeID → session` 注册表
3. 双方在这条连接上跑 **yamux**，两端都能开流
4. 叶子保持这条会话常驻，断了按退避重连，封顶 60 秒

head 下发给中继的是 `nodeID → sha256(key)` 而不是 key 本身，
这样即使中继的状态文件被读走，也不会泄露所有节点的凭证。

**叶子在每个拨得到的中继上都安家**，不只是当前路由点名的那个。中继只能在
**已经存在**的会话上推流量 —— 备用中继如果目标从没在上面安过家，就是形同虚设。
代价是每个中继一条空闲 TCP。

### 转发

叶子 A 要连叶子 B 的 `10.88.0.5:5672`：

1. A 上的 sing-box 把这个地址路由到 knot 的本地 SOCKS 监听
2. knot 在自己的会话上开一个流，写 `OPEN{dstNodeID, dstAddr}`
3. 中继查到 B 的会话，在**那条会话上**开一个流，把同样的头原样传过去
4. B 在本地拨 `dstAddr` 并应答
5. 中继在两个流之间 splice，不解析内容

**中继自己作为发起方时直接用自己的注册表**，不会去绕另一个中继。

### 两个看着不起眼但很要紧的细节

**确定路径可用之后才回 SOCKS 应答。** 先回「成功」再发现对端不可达，
会把每个路由配错都变成「TCP 连上了但一个字节都不来」—— 这个症状指向的方向
和真正的原因毫无关系。它还会让 `nc -z` 撒谎：只完成握手的探测会把根本不通的
对端报成 up。

**失败要带原因。** `OPEN` 的应答是 OK 或一个失败字符串，调用方可以立刻换下一条
路径而不是等超时，日志里也能看出是哪一端拒绝的。

### 健康与故障

- yamux keepalive 设 15 秒（默认 30 秒会让半开会话在跨境链路上留得太久）
- 节点重连时替换旧会话，并把旧的关掉，让它的流立即释放而不是等超时
- 用某个中继之前先看会话活没活 —— 主中继挂了不用每条连接付一次拨号超时
- `OPEN` 走哪个中继由 head 下发的路由策略决定，排序和切换在叶子侧做

## 限制

**只搬流，不搬数据报。** 走中继的 UDP 不可能通，所以在路由层就显式丢掉，
而不是交给一个会对每个 UDP-ASSOCIATE 回拒绝、把日志刷满的 SOCKS 出站。

**只有单跳。** `A → 中继1 → 中继2 → B` 没有实现。中继在自己注册表里找不到
`dstNodeID` 就直接让 `OPEN` 失败，不会再转发。协议本身留了余地 ——
中继是按 node ID 而不是地址路由的 —— 只是暂时没有需求。

## 这让 sing-box 处在什么位置

sing-box 现在负责 TUN、Reality 传输和路由。既然中继路径已经不依赖它的出站选择
逻辑，把它换成 `github.com/sagernet/sing-tun` 加一个 Reality 库，就能彻底去掉
这个子进程。

**目前不值得。** CLI 和配置 schema 是稳定接口，它的 Go API 不是。
